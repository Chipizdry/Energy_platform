name: Deploy

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'cor_pass/version.py'  # чтобы не зациклиться

permissions:
  contents: write  # даём Actions право пушить

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣ Клонируем репозиторий
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0
        persist-credentials: false  # важно! чтобы использовать токен

    # 2️⃣ Инкрементируем build_number и создаём version.py
    - name: Increment build number
      id: build
      run: |
        VERSION_FILE="cor_pass/version.py"

        if [ -f "$VERSION_FILE" ]; then
          CURRENT=$(grep "__version__" $VERSION_FILE | sed -E "s/__version__='0\.1\.([0-9]+)'/\1/")
          BUILD_NUMBER=$((CURRENT + 1))
        else
          BUILD_NUMBER=1
        fi

        VERSION="0.1.${BUILD_NUMBER}"
        echo "VERSION=$VERSION" >> $GITHUB_ENV

        echo "__version__='${VERSION}'" > $VERSION_FILE
        echo "✅ New version: $VERSION"
        cat $VERSION_FILE

    # 3️⃣ Коммитим и пушим version.py обратно в main
    - name: Commit and push version file
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add cor_pass/version.py
        git commit -m "🔼 Auto bump version to ${VERSION}" || echo "No changes to commit"
        git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git HEAD:main

    # 4️⃣ Устанавливаем зависимости
    - name: Install dependencies
      run: pip install -r requirements.txt

    # 5️⃣ Деплой (пример)
    - name: Deploy
      run: |
        echo "🚀 Deploying version $VERSION"
